# Legacy

Frontend original en Flet 0.28.3, conservado como referencia durante la migración a React.

Se arranca con:

```powershell
pip install -r legacy/requirements-frontend.txt
flet run legacy/frontend-flet/main.py
```
